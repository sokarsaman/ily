name: las

on: 
  push:
    branches:
      - main

jobs:
  message:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello my name is sokar, My Age is ${{vars.Age}}"